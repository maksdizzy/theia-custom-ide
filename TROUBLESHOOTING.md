# Troubleshooting Guide - Flexbe IDE

## 🔥 Частые проблемы и решения

### Проблема 1: Сайт недоступен после запуска

**Симптомы:**
- Команда `npm run start` выполняется, но http://localhost:4000 недоступен
- Turbo показывает "cache hit, replaying logs"
- Порт 4000 свободен, но сервер не отвечает

**Причина:**
Turbo кеширует результаты команды `start` и воспроизводит СТАРЫЕ логи вместо реального запуска сервера.

**Решение:**
Используйте обновлённые скрипты, которые обходят Turbo:

```bash
npm run quick-start
# или
npm run full-start
```

Или запустите вручную:
```bash
cd browser-app
node lib/backend/main.js ../workspace -p 4000 --plugins=local-dir:../plugins
```

**НЕ используйте** `npm run start` напрямую - используйте `quick-start` или `full-start`!

---

### Проблема 2: Ошибка "EADDRINUSE: address already in use"

**Симптомы:**
```
Error: listen EADDRINUSE: address already in use 127.0.0.1:4000
```

**Причина:**
Другой процесс уже использует порт 4000.

**Решение:**
```bash
# Остановить процесс на порту 4000
lsof -ti :4000 | xargs kill

# Или использовать full-start (остановит автоматически)
npm run full-start
```

---

### Проблема 3: "Cannot GET /" или HTTP 404

**Симптомы:**
- Сервер запущен на порту 4000
- Браузер показывает "Cannot GET /" или 404

**Причина:**
Frontend не собран (отсутствует `browser-app/lib/frontend/`).

**Решение:**
```bash
npm run rebuild
npm run quick-start
```

---

### Проблема 4: "Module not found @flexbe/custom-ui"

**Симптомы:**
```
Module not found: Error: Can't resolve '@flexbe/custom-ui/lib/frontend/index.js'
```

**Причина:**
custom-ui не собран или собран неправильно.

**Решение:**
```bash
# Пересобрать в правильном порядке
cd custom-ui
npm run build

cd ../browser-app
npm run build

cd ..
npm run quick-start
```

---

### Проблема 5: Правая панель не видна

**Симптомы:**
- IDE запускается
- Правая боковая панель отсутствует

**Причина:**
- Фикс ещё не применён
- Или старая версия собрана

**Решение:**
```bash
# Убедитесь что фикс применён
grep "0.25" custom-ui/src/frontend/layout/application-shell.ts

# Должно быть: [0, 1, 0.25], // Right panel gets 25% width

# Если нет, применить фикс и пересобрать
npm run rebuild
npm run quick-start
```

---

### Проблема 6: node-gyp ошибки при установке

**Симптомы:**
```
gyp ERR! Python version mismatch
```

**Причина:**
Python 3.14+ не поддерживается node-gyp (отсутствует `distutils`).

**Решение:**
```bash
# Проверить текущую версию
npm config get python

# Настроить Python 3.10
echo "python=/opt/homebrew/bin/python3.10" > .npmrc

# Переустановить зависимости
rm -rf node_modules
npm install
```

---

## 🛠️ Полезные команды для диагностики

### Проверка статуса сервера

```bash
# Проверить, запущен ли сервер
lsof -i :4000

# Проверить процессы Theia
ps aux | grep "theia start"

# Тест HTTP ответа
curl -I http://localhost:4000/
```

### Проверка сборки

```bash
# Проверить custom-ui
ls -la custom-ui/lib

# Проверить browser-app frontend
ls -la browser-app/lib/frontend

# Проверить browser-app backend
ls -la browser-app/lib/backend
```

### Очистка и пересборка

```bash
# Полная очистка
npm run clean
npm install

# Пересборка всего
npm run build

# Или через скрипт
npm run rebuild
```

---

## 📝 Правильный порядок команд

### Первый запуск:
```bash
npm install
npm run download:plugins
npm run full-start
```

### После изменений в custom-ui:
```bash
npm run rebuild
npm run quick-start
```

### После git pull:
```bash
npm run full-start  # Проверит и пересоберёт автоматически
```

### Watch mode для разработки:
```bash
# Терминал 1
cd custom-ui && npm run watch

# Терминал 2
npm run quick-start
```

---

## 🔍 Логи и отладка

### Посмотреть логи Turbo:
```bash
cat .turbo/turbo-*.log
```

### Запуск с отладочным выводом:
```bash
cd browser-app
DEBUG=* node lib/backend/main.js ../workspace -p 4000 --plugins=local-dir:../plugins
```

### Проверить Turbo кеш:
```bash
# Очистить кеш Turbo
rm -rf .turbo

# Запустить без кеша
turbo start --force
```

---

## ❓ Всё ещё не работает?

1. **Проверьте версии:**
   ```bash
   node --version  # Должно быть >= 20
   npm --version   # Должно быть >= 10
   ```

2. **Полная переустановка:**
   ```bash
   npm run clean
   rm -rf node_modules package-lock.json
   npm install
   npm run download:plugins
   npm run rebuild
   npm run quick-start
   ```

3. **Проверьте порты:**
   ```bash
   lsof -i :4000  # IDE
   lsof -i :3000  # Может конфликтовать
   ```

4. **Проверьте логи:**
   - Логи Turbo в `.turbo/`
   - Логи сервера в терминале
   - Консоль браузера (F12)

---

**Если ничего не помогает**, откройте issue в репозитории или обратитесь к документации в `claudedocs/`.
