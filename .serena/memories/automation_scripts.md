# Automation Scripts for Flexbe IDE

**Created**: 2026-01-04  
**Purpose**: Automated build and start scripts for development workflow

## Scripts Created

### 1. `scripts/full-start.sh` (npm run full-start)

**Полный запуск с проверками и автоматическими пересборками.**

**Функциональность:**
- ✅ Проверяет порт 4000 (предлагает остановить занятый процесс)
- ✅ Проверяет node_modules (устанавливает, если отсутствуют)
- ✅ Проверяет сборку custom-ui (пересобирает при необходимости)
- ✅ Проверяет сборку browser-app (пересобирает при необходимости)
- ✅ Сравнивает timestamp файлов для определения устаревших сборок
- ✅ Предлагает скачать плагины, если отсутствуют
- ✅ Запускает сервер на http://localhost:4000

**Использование:**
```bash
npm run full-start
# или
./scripts/full-start.sh
```

**Когда использовать:**
- Первый запуск после клонирования
- После изменений в custom-ui/src/
- Если не уверены в состоянии проекта

---

### 2. `scripts/quick-start.sh` (npm run quick-start)

**Быстрый запуск без проверок.**

**Функциональность:**
- ⚡ Останавливает процесс на порту 4000 (если есть)
- ⚡ Сразу запускает сервер

**Использование:**
```bash
npm run quick-start
# или
./scripts/quick-start.sh
```

**Когда использовать:**
- Проект уже собран
- Нужно быстро перезапустить

---

### 3. `scripts/rebuild-all.sh` (npm run rebuild)

**Полная пересборка без запуска.**

**Функциональность:**
- 🔨 Останавливает сервер (если запущен)
- 🔨 Пересобирает custom-ui
- 🔨 Пересобирает browser-app

**Использование:**
```bash
npm run rebuild
# или
./scripts/rebuild-all.sh
```

**Когда использовать:**
- После изменений в исходниках
- Для принудительной пересборки
- Перед коммитом

---

## NPM Scripts Added

Добавлены в `package.json`:
```json
{
  "scripts": {
    "full-start": "./scripts/full-start.sh",
    "quick-start": "./scripts/quick-start.sh",
    "rebuild": "./scripts/rebuild-all.sh"
  }
}
```

---

## Development Workflows

### Workflow 1: Watch Mode (Recommended)

**Терминал 1:**
```bash
cd custom-ui
npm run watch
```

**Терминал 2:**
```bash
npm run quick-start
```

Изменения в custom-ui автоматически пересобираются. Обновите браузер.

---

### Workflow 2: Manual Rebuild

После изменений в custom-ui:
```bash
npm run rebuild
npm run quick-start
```

---

## Smart Features

### Timestamp-Based Rebuild Detection

Скрипты проверяют timestamp директорий:
- Если `custom-ui/src` новее `custom-ui/lib` → пересборка custom-ui
- Если `custom-ui/lib` новее `browser-app/lib` → пересборка browser-app

### Port Conflict Resolution

Автоматически обнаруживает и предлагает остановить процессы на порту 4000.

### Colored Output

Использует ANSI цвета для понятного вывода:
- 🔵 Синий: информация
- 🟢 Зелёный: успех
- 🟡 Жёлтый: предупреждение
- 🔴 Красный: ошибка

---

## Build Order Requirements

**Критично:** custom-ui должен собираться ДО browser-app!

```
custom-ui/src/  →  custom-ui/lib/  (Vite)
       ↓
custom-ui/lib/  →  browser-app (импорт)
       ↓
   browser-app  →  browser-app/lib/  (Webpack)
```

Скрипты автоматически соблюдают этот порядок.

---

## Documentation

Создана `scripts/README.md` с полной документацией:
- Описание всех команд
- Workflow для разработки
- Troubleshooting
- Полезные команды

---

## Usage Examples

**Первый запуск:**
```bash
npm install
npm run download:plugins
npm run full-start
```

**Обычная разработка:**
```bash
# Режим watch (рекомендуется)
npm run watch    # Terminal 1
npm run quick-start  # Terminal 2

# Или ручная пересборка
npm run rebuild
npm run quick-start
```

**После git pull:**
```bash
npm run full-start  # Проверит и пересоберёт при необходимости
```

---

## Files Created

1. `scripts/full-start.sh` - Полный запуск с проверками
2. `scripts/quick-start.sh` - Быстрый запуск
3. `scripts/rebuild-all.sh` - Пересборка
4. `scripts/README.md` - Документация
5. `package.json` - Обновлён (добавлены npm scripts)

Все скрипты имеют права на выполнение (`chmod +x`).
