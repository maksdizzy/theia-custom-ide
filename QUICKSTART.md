# 🚀 Flexbe IDE - Quick Start

## Первый запуск

```bash
npm install
npm run download:plugins
npm run full-start
```

Откройте http://localhost:4000 в браузере.

---

## Ежедневное использование

### Вариант 1: Watch Mode ⭐ Рекомендуется

**Терминал 1:**
```bash
cd custom-ui && npm run watch
```

**Терминал 2:**
```bash
npm run quick-start
```

После изменений обновите браузер (`Cmd+R`).

---

### Вариант 2: Ручная пересборка

```bash
npm run rebuild
npm run quick-start
```

---

## 📋 Основные команды

| Команда | Описание |
|---------|----------|
| `npm run full-start` | ✨ Полный запуск с проверками (рекомендуется) |
| `npm run quick-start` | ⚡ Быстрый запуск без проверок |
| `npm run rebuild` | 🔨 Пересобрать custom-ui + browser-app |
| `npm run build` | 📦 Собрать всё через Turbo |
| `npm run watch` | 👀 Режим разработки (следит за изменениями) |
| `npm run download:plugins` | 📥 Скачать VS Code плагины |

---

## 🛠️ Если что-то не работает

### Сервер не запускается (порт занят)

```bash
lsof -ti :4000 | xargs kill
npm run quick-start
```

### Ошибка 404 или "Cannot GET /"

```bash
npm run rebuild
npm run quick-start
```

### Ошибка "Module not found @flexbe/custom-ui"

```bash
cd custom-ui && npm run build
cd ../browser-app && npm run build
npm run quick-start
```

---

## 📚 Дополнительная документация

- [scripts/README.md](scripts/README.md) - Подробная документация по скриптам
- [claudedocs/](claudedocs/) - Техническая документация проекта
- [custom-ui/](custom-ui/) - Кастомизация UI
- [browser-app/](browser-app/) - Главное приложение

---

## 🔧 Полезные команды

```bash
# Остановить сервер
lsof -ti :4000 | xargs kill

# Проверить статус порта
lsof -i :4000

# Полная очистка
npm run clean
npm install

# После git pull
npm run full-start
```

---

**Документация создана Claude Code** 🤖
