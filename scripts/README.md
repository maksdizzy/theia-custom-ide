# Скрипты для Flexbe IDE

Автоматизированные скрипты для сборки и запуска IDE.

## 🐳 Docker (рекомендуется)

### Первый запуск

```bash
docker compose up -d
```

Образ автоматически соберётся (~5-10 мин первый раз) и запустится.

### Команды Docker

```bash
# Запуск IDE
npm run start:docker     # или: docker compose up -d

# Остановка
npm run docker:down      # или: docker compose down

# Логи
npm run docker:logs      # или: docker compose logs -f

# Пересборка образа
npm run docker:build     # или: docker compose build

# Полная пересборка (без кеша)
npm run docker:rebuild   # или: docker compose build --no-cache
```

IDE доступна на http://localhost:4000

---

## 💻 Локальный запуск

### Первый запуск

```bash
npm install
npm run download:plugins
npm run start
```

### Команды

| Команда | Описание |
|---------|----------|
| `npm run start` | Полный запуск с проверками сборки |
| `npm run start:quick` | Быстрый перезапуск (без проверок) |
| `npm run start -- --build` | Принудительная пересборка |
| `npm run build` | Сборка всех workspace |
| `npm run watch` | Режим watch для разработки |

### Прямой запуск скрипта

```bash
./scripts/start.sh              # Полный запуск
./scripts/start.sh --quick      # Быстрый запуск
./scripts/start.sh --build      # С пересборкой
./scripts/start.sh --docker     # Через Docker
./scripts/start.sh --help       # Справка
```

---

## 🔧 Конфигурация

### Environment Variables

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Доступные переменные:

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `PORT` | 4000 | Порт сервера |
| `HOST` | 0.0.0.0 | Хост сервера |
| `WORKSPACE_PATH` | /workspace | Путь к workspace |
| `PLUGINS_DIR` | ./plugins | Директория плагинов |

---

## 🛠️ Workflow для разработки

### Watch Mode (рекомендуется)

**Терминал 1 - Watch custom-ui:**
```bash
npm run watch --workspace=custom-ui
```

**Терминал 2 - IDE:**
```bash
npm run start:quick
```

Изменения в `custom-ui/src/` автоматически пересобираются.
Обновите страницу в браузере (`Ctrl+R` / `Cmd+R`).

---

## 📝 Решение проблем

### Port 4000 already in use

```bash
# start.sh автоматически останавливает процесс на порту
npm run start

# Или вручную:
lsof -ti :4000 | xargs kill
```

### Module not found @flexbe/custom-ui

```bash
npm run start -- --build
```

### node-gyp / Python ошибки

Для сборки native modules нужен Python 3.10-3.12:

```bash
# Установить переменную для одного запуска
npm_config_python=/usr/bin/python3 npm install

# Или раскомментировать строку в .npmrc
```

---

## 📂 Структура

```
scripts/
├── start.sh            # Единый скрипт запуска
├── patch-ai-plugins.js # Патчи для AI плагинов
└── README.md           # Эта документация
```

---

## ⚙️ Порядок сборки

```
custom-ui/src/  →  custom-ui/lib/  (Vite)
        ↓
browser-app     ←  imports @flexbe/custom-ui
        ↓
browser-app/lib/  (Webpack)
```

**Важно:** custom-ui собирается ДО browser-app (Turbo учитывает зависимости).
